# 本地 Web 安全自查与修复台（含合规漏洞赏金模式）— PRD v0.2

## 1. 项目目标

为本机代码项目和已授权的本地靶场提供安全自查、发现分诊、修复跟踪与回归验证闭环；并提供受严格范围闸门约束的漏洞赏金项目规则管理、低影响检查与脱敏报告预检。

系统默认只服务于已登记的本地目标，不是通用公网扫描器，也不对未授权目标执行检查。公网检查仅在漏洞赏金合规模式下、对已登记且项目规则当前明确授权的单个资产执行。

## 2. 用户与运行边界

- 用户：单个本机开发者/测试者。
- 系统：Windows + Edge。
- 服务绑定：仅 `127.0.0.1`。
- 目标范围：
  - 已登记的 `localhost` 服务；
  - 明确允许的本地 Docker 网络地址；
  - 已登记的本地代码目录。
  - 漏洞赏金模式：用户已登记、并由当前项目规则明确列为 In scope 的第三方 URL、域名或 API。
- 目标外行为：不得扫描公网、局域网其他设备、未登记地址或任意磁盘目录；不得自动发现、推断或批量枚举第三方资产。
- 赏金模式前提：每个资产均需保存项目链接、规则核验时间、In-scope 依据、自动化许可、速率限制、禁止项、测试账号条件与报告渠道。规则未明确允许的行为默认禁止。

## 3. 本期功能范围

### 3.1 项目与资产

- 支持将自查台自身登记为项目并执行检查。
- 自动发现常见本地项目、运行中的本地服务和 Docker 容器；发现失败时支持手动录入。
- 可手动登记页面、API、数据类型、依赖与 Docker 服务。
- 支持将漏洞赏金项目登记为独立项目，并为每个项目维护规则链接、规则快照元数据、In-scope 资产、测试限制、保密要求与报告渠道；不抓取或猜测未登记资产。
- 可读取或导入 OpenAPI、`requirements.txt`、`package.json`、`docker-compose.yml`。
- 目标文件不存在时跳过对应导入，并在结果中记录原因。

### 3.2 权限矩阵

- 手工维护角色、资源/API、预期允许/拒绝规则与测试账号映射。
- 支持 Cookie 登录和 Bearer Token 两种认证方式。
- 使用专门创建的本地测试账号。
- 若实际访问结果违背预期规则，例如普通用户 B 能访问用户 A 的订单，创建高优先级“待确认”发现。

### 3.3 安全检查

首版包含以下五类：

1. 密钥泄露：检查代码、配置文件与 Git 历史中的 API Key、密码、Token 等可疑敏感信息。
2. 依赖风险：检查 Python/pip 与 Node/npm 的依赖版本；联网漏洞查询默认关闭，启用时只发送依赖名称和版本。
3. 基础配置：检查调试模式、默认账户、`.env` 误提交、Docker 高权限运行等。
4. HTTP 基线：仅检查登记的本地 URL，关注安全响应头、错误页信息暴露与 HTTPS 状态；本地 HTTP 仅提示，不直接判为漏洞。
5. 权限矩阵回归：以不同测试账号验证服务端是否正确拒绝无权操作。

漏洞赏金模式只可在项目规则明确允许时，对已登记第三方资产执行低影响 HTTP 基线、公开页面/接口一致性检查或经允许的测试账号回归。自动化结果仅为“待确认”线索，必须人工复核，且不得收集真实用户数据或执行主动攻击、爆破、拒绝服务、资产发现或批量扫描。

缺少相应文件或技术栈时，检查应跳过并留存原因，而非报错中断全部运行。

### 3.4 发现、修复与回归

- 发现状态：待确认、已确认、误报、已修复。
- 每条发现至少包含：
  - 标题、受影响模块、发现类型、严重性；
  - 证据与测试时间；
  - 预期行为与实际行为；
  - 影响范围、根因、修复建议；
  - 回归验证结果与状态。
- 修复任务包含负责人、截止时间、根因与修复建议。
- 关闭发现前必须重新运行对应检查并记录回归结果。

### 3.5 历史、备份与报告

- 数据按项目隔离，存储于本机 SQLite。
- 默认保留检查、发现、修复与验证历史。
- 支持手动删除历史数据。
- 支持导出/导入脱敏 JSON 备份。
- 首版支持导出 Markdown 安全自查报告与修复清单。
- 赏金模式支持生成脱敏报告草稿预检：核验范围依据、复现前提、最小化证据、影响说明、敏感数据移除状态与项目指定报告渠道；不自动向任何平台或第三方提交。

## 4. 数据与隐私要求

- 测试账号凭据默认仅存入 Windows 系统凭据库。
- 凭据、密码、Token、Cookie 不得写入 SQLite、项目文件、日志、Git、报告或备份。
- 支持每次测试时手动输入临时凭据。
- 漏洞查询必须经用户明确启用，且不得上传源码、配置、凭据或用户数据。
- 赏金项目规则元数据可在用户明确启用时联网查询或手工录入；不得上传源码、配置、凭据、Cookie、Token、个人数据或完整敏感响应。规则变更或无法重新核验时，禁止继续外部检查。

## 5. 页面范围

1. 项目总览：风险数量、修复率、最近一次检查。
2. 资产页：本地服务、代码仓库、接口、依赖、Docker 服务。
3. 权限矩阵页：角色、资源/API 与预期规则。
4. 发现页：证据、严重性、状态、修复建议与回归记录。
5. 报告页：Markdown 报告、脱敏 JSON 导入/导出。
6. 赏金项目页：项目规则、范围、限制、核验时间、允许检查和报告预检状态。

## 6. 本期不做

- 未经项目明确授权的公网或非本机目标扫描。
- 自动资产发现、批量枚举、端口范围扫描、爆破、拒绝服务、规避访问控制、接触真实用户数据或自动提交第三方报告。
- Basic Auth 自动化测试。
- PDF 报告导出。
- AI 自动判定漏洞。
- 上传源码、配置、凭据或用户数据。
- 防御本机管理员、恶意软件或已失陷操作系统。

## 7. 技术与部署约束

- 后端：Python + FastAPI。
- 数据库：SQLite。
- 前端：简单网页，首版不引入 React。
- 容器：Docker，供项目运行及本机靶场测试使用。
- 浏览器：Edge。
- 网络：默认离线；仅在用户启用依赖漏洞查询或赏金项目规则核验时访问公开服务。对第三方资产的网络请求必须通过范围闸门，并遵守该项目明确允许的方式和速率。

## 8. 安全边界

- 自查台使用独立登录保护，不复用测试账号。
- 前端或本地登录保护不能等同于服务端认证或加密存储。
- 系统主要降低误操作和普通本机用户的未授权访问风险。
- 漏洞赏金模式不保证奖金、报告受理或法律保护；具体项目页面的范围、资格、规则和 Safe Harbor 高于本地记录。
- 不承诺抵御拥有系统管理员权限的用户、恶意软件或操作系统层面的入侵。

## 9. 验收标准

- 可登记、自动发现或手动录入本地项目与资产。
- 五类检查均可执行、跳过不适用项并保存可追溯证据。
- 权限矩阵可通过 Cookie 和 Bearer Token 验证允许/拒绝规则。
- 凭据、Token、Cookie 不出现在数据库、日志、Git、报告或备份中。
- 使用自查台自身、一个 FastAPI 靶场和一个 Node.js 靶场，验证每类检查均能完成“发现→修复任务→修复→回归关闭”。
- 可导出 Markdown 报告与脱敏 JSON 备份，并可导入备份。
- 默认不联网查询漏洞库；启用后仅发送依赖名称与版本。
- 赏金模式在规则明确允许时，才可对已登记的 In-scope 资产执行允许的低影响检查；范围、速率、禁止项和脱敏报告预检均须有可追溯记录。
- 每项验收标准均须具备自动化执行的真实证据，或明确记录未验证原因与残余风险。

## 10. 验证策略

- 默认由自动化执行助手完成测试、静态检查、范围校验与可执行的本地回归验证，并记录真实命令、时间、结果、证据与跳过原因。
- 用户不承担手工测试责任；用户确认仅用于需求、文件范围、风险、具有副作用的操作及验收结论。
- 每个小项目仍必须具备完成定义和自动化测试步骤；在已确认开发计划和文件范围内，自动化验证、静态检查与 `git diff --check` 均实际通过后，自动创建仅包含该小项目文件的本地 Git 提交并进入下一项。
- 自动推进不包含 `git push`；遇到范围扩大、安装或升级依赖、配置或锁文件修改、删除或批量操作、联网或外部影响、测试失败、测试无法执行或安全风险时，必须暂停并等待用户确认。
- 无法自动验证的浏览器交互、Docker 实际运行、操作系统凭据库等项目，必须明确标记为“未验证”，不得视为通过。
- 赏金模式的终验必须使用本地模拟规则和测试靶场验证范围闸门、速率限制、跳过逻辑、敏感字段脱敏与人工复核门槛；不得把对真实第三方目标的检查作为自动化验收前提。
- 不得以未实际运行的测试、扫描或构建结果作为验收证据。
- 所有已确认小项目完成后，自动执行 PRD 终验并输出证据、未验证项与残余风险；只有验收标准均满足且终验结论为通过，才标记为“可交工”。

## 11. 已知假设与未决项

- 默认使用 Docker Desktop；若本机未具备该环境，在环境准备阶段重新确认替代方案。
- 自动发现仅限常见项目目录、运行中的本地服务及 Docker 容器，不扫描整个磁盘。
- 自查台独立登录保护的具体认证与恢复机制，将在技术方案确认后定义。
