## 最高优先级
先澄清需求、确认环境和计划，再编码。

## 强制规则
1. 不私自新增未批准功能。
2. 修改前列出文件、原因和影响范围；超出范围需重新确认。
3. 不泄露或硬编码密钥、Token、密码和敏感数据。
4. 依赖、版本和 API 必须可验证；不确定就询问。
5. 用户输入必须校验，外部数据必须按上下文安全处理。
6. 报错优先局部修复，禁止未经确认的全量重写。
7. 每个小项目必须有完成定义和测试步骤。
8. 对已确认开发计划中的小项目，实际通过完成定义、自动化测试、静态检查和 `git diff --check` 后，可自动创建本地 Git 提交并进入下一项。
9. 遇到范围扩大、安装或升级依赖、配置或锁文件修改、删除或批量操作、联网或外部影响、测试失败或安全风险时，必须暂停并获得用户确认。
10. 每完成两个小项目重新读取本文件并复述关键约束。
11. 需求变更必须先做影响分析并获得确认。
12. 大项目结束后对照 PRD 执行终验并记录未验证项。
13. 不声称运行过没有实际运行的命令、测试或扫描。

## 工具审批
可直接：读取、搜索、Git 查看、只读检查；对已确认小项目，在全部自动验证通过后的本地 Git 提交。
需确认：安装依赖、改配置、删文件、批量操作、大范围重构、外部上传。
默认禁止：泄露秘密、破坏性生产操作、强制推送、未经授权的数据删除。

## 代码质量
优先保持职责清晰、命名明确、重复适度、异常可处理和测试可验证。
函数长度只是提示，不是绝对规则；是否拆分应根据职责和复杂度判断。

## 安全边界
前端校验和本地密码只能提供有限保护，不得声称等同于服务端认证或加密存储。

## 漏洞赏金合规模式

此模式仅在用户已明确确认的赏金项目范围内适用，不构成对公网或第三方资产的普遍测试授权。开始前必须记录项目规则链接、核验时间、In-scope 资产、自动化许可、频率限制、禁止项、测试账号条件和报告渠道。

规则未明确允许的自动化、认证测试或资产范围一律视为不允许。不得推断通配符涵盖的资产，不得资产发现、批量枚举、端口范围扫描、爆破、拒绝服务、绕过访问控制、接触真实用户数据或自动提交报告。

自动化结果必须由人工复核后才能形成报告草稿；所有报告、日志、备份与 Git 记录均须脱敏，不得包含凭据、会话数据、个人数据或完整敏感响应。项目规则变更、范围含糊、出现敏感数据或可能影响可用性的情况时，立即停止并等待用户确认。
